{
  "name": "npm-first",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "test": "jest --config jestconfig.json",
    "lint": "tslint -p tsconfig.json",
    "build": "tsc",

    "prepare": "yarn build",
    "prepublishOnly": "yarn test && yarn lint",
    "preversion": "yarn lint",
    "version": "yarn format && git add -A src",
    "postversion" : "git push && git push --tags" 
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coding-2023/npm-01.git"
  },
  "files": [
    "lib/**/*"
  ],
  "keywords": [],
  "author": "V-T",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.7",
    "ts-jest": "^29.1.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^5.0.4"
  }
}
